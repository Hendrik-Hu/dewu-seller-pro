# v0.20.1 RC audit patch

- Removed the unused legacy `lookup-sku` Edge Function from both the deployable source and the live project.
- Reduced authenticated `fee_schemes` grants to row-scoped CRUD; `TRUNCATE`, `REFERENCES` and `TRIGGER` are revoked.
- Revoked direct client execution of four database trigger implementation functions.
- Re-ran account deletion, inventory operations, backup/restore, fee, settlement, warehouse and large-ledger rollback checks.
- Replaced the long-term v0.20 profile evidence with a disposable-account screenshot so release archives contain no real email address.

Public launch remains blocked on a user-owned domain and custom SMTP. Production remains a closed test with email auto-confirm enabled.
